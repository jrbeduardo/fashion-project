version: '3'
services:
  api:
    image: api-fashion
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - 80:80
  frontend:
    image: fontend-ropa
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - 82:80
    depends_on:
      - api